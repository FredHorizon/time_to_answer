$('#avatarModal').modal('hide');

document.getElementById('imgAvatarLg').src = "<%= url_for(@user.user_profile.avatar) %>" <!-- url_for extrai a url do modelo entre os parênteses -->
document.getElementById('imgAvatarMd').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('imgAvatarSm').src = "<%= url_for(@user.user_profile.avatar) %>"

$.bootstrapGrowl('Imagem atualizada com sucesso!', {
  type: 'success', // (null, 'info', 'danger', 'success')
  align: 'right', // ('left', 'right', or 'center')
  allow_dismiss: true, // If true then will display a cross to close the popup.
  stackup_spacing: 10 // spacing between consecutively stacked growls.
});
